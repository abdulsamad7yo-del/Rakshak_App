import React from "react";
import { View, ScrollView} from "react-native";
import Trustedcontactexport from "../screens/Trustedcontact(export)";
export default function Trustedcontacts() {
    return (
        <ScrollView>
        <View >
            <Trustedcontactexport/>
        </View>
        </ScrollView>
    );
}